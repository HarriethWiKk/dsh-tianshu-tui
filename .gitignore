node_modules
# tsc intermediates only — lib/index.js and lib/invariant.js must be tracked
# so github: installs do not rebuild inside the harness workspace (issue #1).
lib/types/**/*.js
lib/types/**/*.js.map
*.tgz
.DS_Store
coverage/
.npmrc

# scratch 目录（superpowers 工作流产物；有价值文档已归档进 docs/）
.superpowers/
docs/superpowers/

# rivet 工具本地状态（每台机器各自生成，不入库）
.rivet/
.rivet.md
.rivet-config.json
