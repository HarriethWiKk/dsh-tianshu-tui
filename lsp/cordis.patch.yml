# dsh bundle patch: inserts the LSP plugin into a profile's layer stack.
# 装配：npx -y @deepseek-ai/dsh plugin --profile tui add github:omdsh-dev/dsh-lsp
- insert:
    - id: lsp
      name: '@deepseek-ai/dsh-lsp'
